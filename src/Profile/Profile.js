import React from 'react';

const Profile = (props) => {
    console.log(props);
 
    return (
        <div >
    <img src='Capture.PNG' alt='
    sal'></img>
        </div>

    );
};

export default Profile;